#!/usr/bin/env bash

set -euo pipefail

curl https://github.com/so-dang-cool/dt/releases.atom 2>/dev/null \
  | grep \<id\> \
  | awk '/<id>.+\/v/ { sub(/.+\/v/, ""); sub(/<.+>/, ""); print $1 }' \
  | tac
